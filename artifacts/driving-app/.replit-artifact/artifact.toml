kind = "web"
previewPath = "/"
title = "DriveTrack"
version = "1.0.0"
id = "artifacts/driving-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22329

[services.development]
run = "pnpm --filter @workspace/driving-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/driving-app", "run", "build" ]
publicDir = "artifacts/driving-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22329"
BASE_PATH = "/"
